/* ÆZ Arena — Mobile install prompt intentionally disabled.
   PWA support remains available through the manifest, service worker,
   and the built-in install flow in index.html. */
(function(){
  'use strict';
})();
